spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/abdoulayegaye/config-repo.git
          default-label: main

server:
  port: 8888

